apiVersion: batch/v1
kind: Job
metadata:
  name: log-service-migrate
  namespace: deploy-status-checker
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: 241533152550.dkr.ecr.us-west-2.amazonaws.com/log-service-migrate:v1
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: log-service-cm
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: log-service-secret
                  key: DATABASE_URL
